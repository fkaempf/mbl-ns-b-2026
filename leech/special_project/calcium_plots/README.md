# Calcium plots — all in one place (84 figures)

Every leech-calcium plot from both analysis pipelines, copied here. Filename
prefixes mark the source:

- **`p2_…`** (45) — second pipeline, from `analysis_calcium/plots/`
  (`fig_*`, `s1_*`, `s2_*`, … + `fig_cycle_movie.mp4`).
- **`p1_<set>_<stage>_…`** (34) — my pipeline, from
  `analysis/calcium/regions/helobdella_LeechNo2_elavgcamp6m-17/…`
  (`<set>` = full/small ROIs; `<stage>` = results / results_residual / phase /
  contraction). E.g. `p1_small_phase_ganglion_phase.png`.
- **`diag_…`** (5) — scratch diagnostics (motion map, slow-motion, ROI previews).

These are **copies**; regenerate originals via the scripts in
`analysis/calcium/` and `analysis_calcium/scripts/`.
